{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Voir Contact{% endblock %}

{% block body %}
{{ parent() }}

<h1 class="text-primary text-center">Voir Contact</h1>

<div class="container">
    <div class="table-responsive">  
        <table class="table">     
            <thead>     
                <tr>       
                    <th scope="col">Nom</th>       
                    <th scope="col">Sujet</th>       
                    <th scope="col">Message</th>     
                </tr>    
            </thead>    
            <tbody> 
    {% for contact in contacts %}
            <tr class="{{ cycle(['table-primary', 'table-secondary'], loop.index0) }}">
                <td>{{ contact.nom | capitalize }}</td>
                <td>{{ contact.sujet }}</td>
                <td>{{ contact.message }}</td>
            </tr>
    {% endfor %}        
            </tbody>   
        </table> 
    </div>
</div>    
{% endblock %}
